# Указываем используемый шаблон
FROM python:3.9
# Устанавливаем зависимости нашего приложения (pip3 для linux)
RUN pip3 install requests fastapi uvicorn
# Копируем в образ файлы приложения
COPY . .
# Открываем порт 8000 чтобы он был доступен снаружи контейнера
EXPOSE 8000
# Выполняем команду для запуска приложения внутри контейнера
CMD ["uvicorn", "my_api:app", "--host", "0.0.0.0", "--port", "8000"]
